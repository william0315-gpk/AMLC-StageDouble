# StageDouble —— 现有代码与目标需求的差距分析

## 一、总览

| 对比维度 | 现有代码 | 目标需求 | 差距 |
|---------|---------|---------|------|
| 输入信号 | 声音 + 舞蹈视频，共 115 个数值 | 同上 | 无差距 |
| 输出参数 | 2 个参数 | 6 个参数（嘴巴开合、表情强度、头部角度、身体动作幅度、手臂高度、动作速度） | **需要改** |
| 学习模型 | 最简单的神经网络（10 个神经元） | 更强、更稳定的神经网络 | **需要改** |
| 训练方式 | 示范式学习（已实现） | 同上 | 无差距 |
| 运行速度 | 10 次/秒，够用 | 需要更充分利用 M2 芯片性能 | **需要优化** |
| 前方采集 | 声音采集、视频姿态采集（已完成） | 同上 | 无差距 |
| 后方对接 | 能发送数据，但无人接收 | 对接 Unreal Engine + MetaHuman 或 Ready Player Me | **需要新建** |
| 代码状态 | 写完了，但从未实际运行过 | 需要跑通、测试、调优 | **需要验证** |

---

## 二、具体差距说明

### 差距 1：输出参数不够（2 个 → 6 个）

**现有代码**：只能输出 2 个控制参数，比如只能控制"嘴巴"和"身体"两个维度。

**目标需求**：需要输出 6 个参数，分别控制虚拟人的不同部位：

| 参数 | 说明 |
|------|------|
| 嘴巴开合 | 说话唱歌时嘴型自然张合 |
| 表情强度 | 情绪越强烈，表情越夸张 |
| 头部角度 | 头部随声音高低自然摆动 |
| 身体动作幅度 | 身体随节奏律动 |
| 手臂高度 | 手臂位置随表演变化 |
| 动作速度 | 动作快慢跟随节奏 |

**改动量**：这一项改动很小，改几行代码就行。但会连带影响后续的模型训练和录制命令格式。

---

### 差距 2：学习模型太简单

**现有代码**：用的是最基础的神经网络，只有 1 层、10 个神经元。打个比方，这就像一个只有 10 个脑细胞的大脑，学习能力有限。

**问题**：
- 当需要同时学习 6 个输出时，10 个神经元很难处理这么复杂的映射关系
- 训练数据少的时候（比如只录了十几条示范），结果不稳定，每次训练结果可能不一样
- 没有利用 M2 芯片的图形处理器加速，全部用 CPU 计算

**需要改成**：更多层的神经网络（比如 3 层，分别有 64、32、6 个神经元），并且换用能利用 M2 芯片加速的框架。

**改动量**：这是最大的改动，需要重写模型部分的代码，引入新的工具库。

---

### 差距 3：没有利用 M2 芯片的加速能力

**现有代码**：所有计算都在 CPU 上跑。M2 芯片有很强的图形处理器，但代码完全没有用上。

**需要做的**：
- 换用支持 M2 加速的机器学习框架
- 对于模型推理（从输入预测输出）这一步，用图形处理器比 CPU 快很多

**改动量**：中等，主要是换框架、适配新接口。

---

### 差距 4：没有对接虚拟人渲染引擎

**现有代码**：模型预测出的参数会通过网络发送到某个地址，但没有任何程序在接收这些数据。相当于对着空气说话。

**需要做的**：
- 在 Unreal Engine 里配置接收这些参数的插件
- 把 6 个参数分别映射到 MetaHuman 虚拟人的对应部位（比如"嘴巴开合"参数控制下颌骨骼）
- 确保参数范围合适（比如 0 到 1 之间），虚拟人不会做出奇怪的表情

**调整量**：这部分主要在 Unreal Engine 里完成，Python 代码这边只需要把参数格式调整好即可。

---

### 差距 5：代码从未运行过

**现有代码**：所有代码都写完了，逻辑是完整的，但文件修改时间停在 7 月 17 日，没有任何运行痕迹（没有缓存文件、没有日志输出）。

**需要做的**：
- 在 Mac 上实际运行一次，验证麦克风权限、声音采集是否正常
- 用真实视频跑一遍姿态检测，看看精度和速度
- 完整走一遍"录制→训练→运行"的流程
- 测试在 10 次/秒的频率下是否稳定，会不会卡顿

---

## 三、需要改动的代码文件

| 文件 | 改动内容 | 难度 |
|------|---------|------|
| `ml_trainer_v2.py` | 输出参数 2→6，模型结构重写，换新框架 | 大 |
| `audio_extractor.py` | 基本不需要改 | 无 |
| `motion_extractor.py` | 基本不需要改 | 无 |
| Unreal Engine 项目 | 新建 OSC 接收配置、MetaHuman 参数映射 | 大（UE 侧） |

---

## 四、需要新增的工具

| 工具 | 用途 | 说明 |
|------|------|------|
| PyTorch | 替代现有的简单模型框架 | 支持 M2 芯片加速，训练更稳定，模型可以更复杂 |
| Unreal Engine OSC 插件 | 接收 Python 发来的参数 | UE 内置，无需额外安装，但需要配置 |
| MetaHuman 插件 | 在 UE 中使用高精度虚拟人 | UE 官方免费插件 |

---

## 五、总结

| 类型 | 数量 |
|------|------|
| 完全不用改的部分 | 声音采集、视频姿态采集（2 个文件） |
| 需要改的部分 | 模型训练器（1 个文件）、Unreal Engine 配置 |
| 需要新增的工具 | PyTorch（1 个） |
| 不需要的 | 服务器、GPU 显卡、大型数据集 |

核心就一句话：**前面的采集部分不用动，中间的学习模型需要升级，后面的虚拟人对接需要新建，输出参数从 2 个扩到 6 个。**